ultracode

# Mission: Take the Go HaxBall-clone from solid prototype to production-grade, add rich match stats & play-by-play, and make the AI<=human boundary real and tested — all without changing the physics feel.

You are an autonomous senior Go engineer working in exhaustive multi-agent mode. The repository at `/home/jack/Desktop/hax/haxball-clone` is a HaxBall-style 2D physics football game in Go (module `phootball`, Ebiten for rendering only). Your job is a complete, behaviour-preserving production overhaul plus one new feature: rich per-player/per-team STATS and a chronological PLAY-BY-PLAY event log, persistable per game and surfaced live.

You have NO prior context beyond this prompt and the actual code. Read the real files before changing them. Every claim about line numbers below is approximate — verify against the live source. Symbol names, constants, and numeric values below were captured from a fresh read of the current tree; re-derive exact lines yourself.

---

## REPOSITORY ARCHITECTURE (real packages & files)

- `internal/sim` — authoritative, deterministic simulation. Key files:
  - `match.go` — `Match` struct, `Step(inputs map[int]Intent, deltaTime float64)` tick pipeline, `applyIntent`, `resolveInteractions`, `resetKickoff`, `BuildMatch`/`BuildSolo`/`BuildDuo`/`buildFormation`/`BuildMatchFromConfig`, `applyConfig` (stamps `m.Tuning = cfg.Tuning`). Package consts `obstacleRestitution=0.5`, `netRestitution=0.2`, and in a block `ballWallRestitution=0.90`, `playerWallRestitution=0.50`. **team-possession-charge subsystem** lives here (see below): consts `teamBuildSeconds=1.5`, `teamHoldSeconds=1.5`, `teamDecaySeconds=3.5`, `teamBuildExponent=3.0`, `teamDrainPerSecond=1.0`; funcs `advanceTeamPossession`, `updateBallPossessor`, `advancePossessionBuilder`, `teamPossessionStrength`, `teamBuildCurve`/`teamBuildCurveInv`, `teamCoastEnvelope`, `resetTeamPossession`, `PossessionCharge(side)`, plus the possession-contest helpers `pressuredByOpponent`, `markedByNonBallOpponent`, `ballInTeamPullRange`, `touchingSides`, `engaged`, `touching`, and `inPullRange`/`playerReach` — both now reach via `possessionRadius()` (base `PullRange`, **NOT trap-extended**), NOT `pullRadius()`. (`contestPossession`/`drainTeamPossession` were refactored away.) Fields `possSide`/`possProgress`/`possCoast`/`possessor`/`possBuffDrain`/`possDebuffDrain`/`possContestLatched`/`possBuilder`/`pullSeq` (+ `Player.pullEnterSeq`).
  - `intent.go` — `Intent{Move, Throttle, Aim, ShootHeld, Trap, CancelCharge, Push, AimFromCursor}` — the SOLE per-tick action channel. Note it now has EIGHT fields: `Push` (instant middle-click radial jab) and `AimFromCursor` (human cursor → face-toward-at-TurnRate) were added since this prompt was first written.
  - `view.go` — read-only `View`/`PlayerView`/`BallView`/`FieldView` interfaces + live-match wrappers (`matchView`/`playerView`/`ballView`/`fieldView`, `unwrapPlayer`) — the SOLE observation channel. `View` exposes `Ball`, `Me(id)`, `Carrier`, `Teammates(of)`, `Squad(of)`, `Opponents(of)`, `Field`, `AttackingGoalCenter`, `DefendingGoalCenter`, `KickoffSide`, `Tick`, `Clock`, `Seed() int64`, `BallFriction()`, `Rules`. `PlayerView` exposes — for ALL players — `Velocity()`, `Heading()` (=`p.moveHeading`), `Possession()`, `Stats() PlayerStats`, `ShootCharge()`, `TrapCharge()`, plus the legal `Position/Facing/Radius/ID/Number/Role/Side/HomePosition/Same/SameTeam/AngleToFacing/BallAngleToFacing`. There is NO `SelfView`/`ObservedView` split and NO `NoiseSalt`. `matchView.BallFriction()` returns `v.m.Tuning.BallFriction` (the config value, NOT the live ball body friction).
  - `interaction.go` — ball<->player dribble/attraction/capture/shoot physics. **FROZEN FEEL.** Contains `shoot(player *Player, ball *Ball) bool`, `push(player *Player, ball *Ball) bool` (the middle-click jab: an instant radial shove at `pushPowerFactor=0.7` of front-shot power, fired anywhere within the base `PullRange`), `handleBallToPlayerInteraction(ball, player, dt) (touched bool, bounce float64)`, `handleBallToPlayerAttraction`, `updatePossession`, plus centre-pull, sticky-hold, control-roll, OrbitStick centripetal anti-fling, retention, seat, off-front mass-ratio impulse, and capture-vs-bounce math.
  - `entity.go` — `Ball`, `Player`, `Move`/`rotateToward` turn-rate, charge constants (`shootChargeMax=0.75`, `trapChargeTime=1.25`, `trapChargeDecay=3.2`, `pushFlashSeconds=0.3`; trap-aura shape `trapAuraPeak=0.55`/`trapAuraOverheldFloor=0.5`/`trapAuraReleaseSeconds=0.3`), `NormShootCharge`, `NewBall(position, radius)` = `physics.NewCircleBody(position, radius, -0.3, 1.5)` (friction `-0.3`, mass `1.5` HARD-CODED inside `NewBall`; the ball radius is a PARAMETER, passed as the literal `7.5` at the `BuildMatch`/`BuildSolo`/`BuildDuo` call sites). Player carries new unexported fields `touchCoef`, `boostDrain`, `trapAura`, `wantsPush`, `pushFlash`/`pushFlashPos`, `pullEnterSeq`, `moveHeading`. Two reach accessors: `pullRadius()` = base `PullRange` + `TrapRangeBonus*trapAura` (trap-EXTENDED; used ONLY by the dribble attraction) and **`possessionRadius()`** = `PossessionRange` (falling back to base `PullRange` when ≤0; **NEVER trap-extended**; used by the possession contest); `PushRange()` = base `PullRange`.
  - `scoring.go` — `Touch`, `TouchKind{TouchDribble,TouchKick}`, `ScoreEvent`, `recordTouch(p *Player, kind TouchKind)` (the central touch sink, distinct-toucher collapse, lossy ring cap `touchHistoryCap=8`, sets `LastTouch`, cleared at kickoff), `resolveGoal(goalEntered Side) ScoreEvent`, `findAssist`, `deflectionShooter`; consts `assistWindow=6.0`, `deflectionWindow=0.6`, `touchHistoryCap=8`.
  - `stats.go` — `PlayerStats` = per-player **physics TUNING** block (NOT match stats) + the aim-assist API + `DefaultStats(shootForce)`. The aim-assist API is `func (s PlayerStats) ShootDirection(radial, facing geom.Vec) geom.Vec`, `aimAssistWeight(angle) float64`, `centerPullGrip(possession)`, `stickinessGrip(possession)`, `captureConeRadians(coef)`; plus the `TouchQuality` type (with `captureMul`/`restitutionMul`/`captureConeRadians`) and the fields `ShootAimAssist`/`ShootAimAssistConeRadians`/`ShootAimAssistSoftRadians`, `CaptureConeRadians`/`CaptureConeSoft`, `CenterPullGripFloor`, `PossessionStealRate`, and **NEW `PossessionRange`** (the possession-contest reach, default 5 = base `PullRange`; consumed via `possessionRadius()`). Baseline `Restitution` is now `{InverseQuadraticCurve, front 0.23, back 0.24}` (in `DefaultStats` AND `roles.go fieldPlayerStats`). **FROZEN FEEL.**
  - `curves.go` — `AngleCurve` shapes (`LinearCurve`/`InverseQuadraticCurve`/etc., `ExponentialSteepness=3.0`). **FROZEN FEEL.**
  - `field.go` — `Field` geometry, goal/net build, `CheckGoal`, `GoalOn(side) *Goal`, `goalMouthRange`, `GoalPostRadius=6`, `ConfineBall`/`ConfinePlayer` (wall feel). `Goal.Mouth` is a `physics.Segment` (`goal.go`). **NEW** exported `const PitchLineWidth = 3.0` (the drawn pitch-line width); `CheckGoal` now requires the whole ball to clear the goal line by `±PitchLineWidth` past `Min.X`/`Max.X` (the line is painted into the net, so a goal counts only once the ball is visibly fully over the white line), and `render.go` reads the SAME const so the visible line equals the scored line — a sim↔render coupling.
  - `rules.go`, `phase.go`, `penalties.go` (shootout state machine: `stepShootout`→`stepPenaltyPlay` which DUPLICATES the interaction order + `recordKick(scored bool)`), `zonerule.go` (offside/box keep-out + `ballCarrier() *Player` firm-possession test + **NEW** keeper-only goal-area enforcement `enforceBoxKeeperOnly`, gated in `enforceZoneRules`), `roles.go` (`Role` enum + `StatsForRole` + `fieldPlayerStats()` formation overrides — incl. baseline `Restitution{InverseQuadraticCurve,0.23,0.24}`), `events.go` (`SoundKind`/`SoundEvent`/`emit`/`Sounds`/`DrainEvents`, `ballHitMinSpeed=40.0` — SOUND only, NO recorder), `rng.go` (seeded RNG + `coinToss`), `goal.go`/`score.go`/`team.go`. **There is NO `record.go` and NO recorder/match-stats subsystem of any kind.**
- `internal/physics` — gameplay-agnostic rigid-body engine. **FROZEN FEEL.** `body.go` (`NewCircleBody`, `Body.Update(dt)`: integrate accel → soft speed cap → friction → position), `collision.go` (`Collide(a,b,restitution) bool` returns a woodwork-hit bool; `Resolve(a,b) bool`; `ReflectInside`/`ClampInside` — both UNUSED), `shape.go` (`Circle`/`Segment`/AABB). **No Ebiten allowed here.**
- `internal/geom` — `vec.go` pure vector math. **FROZEN FEEL** for the primitives.
- `internal/control` — the AI. Files: `controller.go` (`Controller interface { Intent(view sim.View) sim.Intent }`), `ai.go` (decision loop, `reactTicks` latency via `a.cached`/`nextDecision`, `applyMoveJitter`, `capAim`, `const aimCapGap = 60.0`, and **NEW** `enforceAbilityExclusivity` — clamps the AI to ONE of `Trap > Push > Shoot` per tick like a human's three mouse buttons, cancelling a live shot charge when a higher-priority ability takes over), `perception.go` (read-only facts; reads `view.Seed()` and opponent hidden state), `predict.go` (closed-form `predictBall`, turn-rate-aware `interceptTime`/`interceptPoint`, `laneSafe`/`ballTravelTime`), `teamplan.go` (`assignRoles`/`electPresser`, `const keeperPressPenalty = 0.7`), `abilities.go` (`shootAt` cross-tick charge controller — sets `CancelCharge=true` on its overtime branch; `launchAligned` uses `Stats().ShootDirection`; `const aimProjectDist = 1000`), `onball.go` (**535-line god-file**: `bestPass`, `bestShot`, `bestDribble`, `clearScore`, `shield`, the action scoring weights and `LastAction()`), `offball.go`, `keeper.go`, `push.go` (NEW — middle-click jab ability; ALSO sets `CancelCharge=true`), `formation.go`, `steering.go` (boids `avoid`), `zones.go`, `tuning.go` (`aiTuning` + `Skill` tiers `SkillEasy`/`SkillNormal`/`SkillHard`/`SkillImpossible`, `DefaultSkill = SkillHard`, `paramsForSkill`, `SkillFromString`; dead fields `shootGap`/`turnTrapSettled`/`trapApproachFactor`), `noise.go` (`hash64` SplitMix64, `noise`/`personality`/`gaussian` — hash-based, no RNG), `geomutil.go`.
- `internal/input` — `human.go` (`Human` controller, WASD/mouse → Intent; ignores the View; depends on `render.ScreenToWorld`; ALREADY sets `Push`, `AimFromCursor=true`, and `CancelCharge` on the right-click rising edge).
- `internal/netcode` — `protocol.go` (`Snapshot` — now also carries presentational `Celebrating`/`PhaseLabel`/`GoalText` for the HUD/overlay — `EntityState{Kind,Position,Facing,Radius,Color,Number,ShootCharge,TrapCharge}`, `ClientMsg`, `SnapshotOf`; still NO `Stats`/`Events`/`ProtoVersion`/`Envelope`/`Hello`), `server.go` (authoritative TCP+gob server, `sanitizeIntent`, `freeSlot`, `Run(ctx)`, `tickLoop` that broadcasts `Encode` ON the tick goroutine after unlocking, `Bot` interface), `client.go` (`Dial`, `Send`, `Snapshot()`, `Close`; a `sync.Mutex` guards only `latest`/`hasSnap`, NOT `Send`/`Close` — the race in D-P0 still stands).
- `internal/render` — `render.go` (draw pipeline + HUD + `ScreenToWorld(x,y int) geom.Vec` STILL via **package-global transform** `view`/`worldW`/`worldH`/`camActive`/`camCenter`/`camZoom` + `BeginUI` — still NO `Viewport` type). **NEW since the rework:** a sized scalable-vector-text API (`textSized`/`TextS`/`TextCenteredS`/`TextRightS`/`MeasureUI`), a single unified HUD — `HUDModel` + `DrawHUD(screen, HUDModel)` built from EITHER `hudFromMatch` (local) OR `HUDFromSnapshot` (client) — plus client FX `drawGoalOverlay`/`drawStageCard`/`advanceFX`/`DrawClientOverlays`, and `icons.go` (procedural `IconPlay`/`IconShield`/`IconClock`/`IconWhistle`/`IconTrophy`/`IconGoalNet`/`IconGear`). STILL TRUE: three full-match draw bodies remain (`Frame`, the now-DEAD `Match`, and `cmd/client/main.go`'s reassembly) and the legacy `Scoreboard`/`ScoreboardWithClock`/`ShootoutPanel`/`CenterBanner` are now dead; extra debug HUD `ShowPossessionBars`/`drawPossessionBarsAll`/`drawTrapAura`/`drawShootCharge`; NO `StatsPanel`. `camera.go` (client-only smoothing; `CameraModeFromName` accepts `ball`/`follow` and `player`/`active` aliases).
- `internal/menu` — `app.go` (AppState machine, owns match/controllers/camera/audio, `afterStep`, `startMatch`; **NEW** tabbed Match Setup `setupTabs` (`setupTeams`/`setupPitch`/`setupBoxes`/`setupRules`) + full `screenResult`; STILL TRUE: `prevState` is a single field not a stack, and Quit-to-Menu sets `a.match=nil` but does NOT clear `a.controllers`), `settings.go` (**NEW** per-team `TeamControl` + `Settings.Teams [2]TeamControl` (Human/AI + HumanSlot + Difficulty + Size), `ClampDependents()`/`Validate()`, `BuildMatch` builds controllers per team; STILL TRUE: `Config()` SWALLOWS the build error → `config.Default()`, and `difficultyPresets = {easy, normal, hard}` omits `impossible`), `ui.go` (immediate-mode widgets + `tabRail`/scroll/`segmented`), **NEW** `theme.go` (`Theme` palette + type scale + `colLayout`) and `result.go` (Result view model `buildResult`).
- `internal/config` — `config.go` (`Config{Geometry,Ruleset,Tuning,Seed}`, `Default()` `Seed=1`), `geometry.go` (`LargeGeometry()` + **NEW** `Geometry.Validate()` relational constraints), `ruleset.go` (**NEW** hybrid `WinFirstAndTimed` + `HybridRuleset`, the `OnDraw []Continuation` draw-resolution chain `ContinueExtraTime`/`ContinueGoldenGoal`/`ContinuePenalties`, `GoldenGoalCapped`/`GoldenGoalSeconds`), `matchsetup.go` (`MatchSetup` now has per-team `HomeSize`/`AwaySize`+`sizes()`, box opponent caps `PenaltyBoxMaxOpp`/`GoalAreaMaxOpp`, `GoalAreaKeeperOnly`, `PenaltyBestOf`, `EvictGrace`; `Validate` is now the SINGLE validator and calls `Geometry.Validate()`/`Normalize()` — D's "single validator" item is DONE), `flags.go` (`ParseGame`/`ParseServer`/`ParseClient`, `validDifficulty` hand-copied skill enum, `EvictGrace = 0.5` magic literal, `Version = "phootball 0.2.0"`), `tuning.go` (`Tuning` + `DefaultTuning()`; **mostly unread by sim — a no-op trap**: only `BallFriction` is read).
- `internal/logging` — `logging.go` (`ParseLevel`/`ParseFormat`); referenced by F, not in the sim feel boundary.
- `cmd/{client,server,game}/main.go` — entry points; each STILL duplicates a `code(err)` exit-mapper + `signal.NotifyContext` scaffold. Plus **NEW** `cmd/screenshots/main.go` (offscreen render tool via `App.DebugRenderScreen`).
- `go.mod` — module `phootball`, `go 1.21`, ebiten v2.7.3; **no `toolchain` directive line**.

There is uncommitted work already on disk (many `internal/control/*.go`, `internal/sim/*.go` are modified/untracked). **Begin by reading the real current state of every file you touch** — do not trust the map's line numbers; re-derive them.

---

## HARD CONSTRAINTS (non-negotiable — read twice)

**(1) DO NOT change the physics behaviour or "feel".** The following are FROZEN — behaviour-preserving refactors (rename, extract, move constants into config *with byte-identical values*, add hooks guarded by `if rec != nil`) are allowed, but ANY change that alters the simulated trajectory, timing, or outcome is forbidden:
   - `internal/sim/interaction.go` (ball<->player dribble/attraction/capture/shoot: centre-pull, sticky hold, control roll, OrbitStick centripetal anti-fling, retention math, seat, off-front mass-ratio impulse, capture-vs-bounce; AND `push()` + `pushPowerFactor=0.7` — the middle-click radial shove).
   - `internal/sim/stats.go` (`DefaultStats(500)` baseline curves AND the aim-assist API — `ShootDirection`/`aimAssistWeight`/`ShootAimAssist`/`ShootAimAssistConeRadians=0.2617993877991494`(~15deg)/`ShootAimAssistSoftRadians=0`, `CaptureConeRadians=0.3839724354387525`(~22deg)/`CaptureConeSoft`, `CenterPullGripFloor=0.65`, `PossessionStealRate=1.0`, `MinShootFactor=0.35`, the `TouchQuality` mapping; baseline `Restitution{InverseQuadraticCurve, 0.23, 0.24}`; `PossessionRange=5`). `ShootDirection` is the single source of truth for launch direction and is ALSO called by `control/abilities.go` and `control/onball.go` — it must stay byte-stable.
   - `internal/sim/curves.go` (`AngleCurve` shapes), `internal/sim/entity.go` charge constants (`shootChargeMax=0.75`, `trapChargeTime=1.25`, `trapChargeDecay=3.2`, trap-aura shape) and `NewBall` physical values (`-0.3`, `1.5`).
   - `internal/sim/roles.go` `fieldPlayerStats()` formation overrides (e.g. `Shoot{Linear,575,172.5}`, `Restitution{InverseQuadraticCurve, 0.23, 0.24}`) used by `BuildMatch` formation players — distinct from the plain `DefaultStats(500)` used by `BuildSolo`/`BuildDuo`.
   - The team-possession-charge subsystem in `match.go` (`teamBuildCurve` = `progress^3`, `teamCoastEnvelope` = `1 - x^2`, `advanceTeamPossession`, the per-player `touchCoef` that `TouchQuality` maps into capture/restitution/cone multipliers inside the contact resolver) — its curves and the per-tick possession ownership are part of the feel. **The possession contest reaches via `possessionRadius()` (base `PullRange`, NEVER trap-extended) in `inPullRange`/`playerReach`** — a trap extends the ball pull but NOT who owns/contests/steals possession; do not revert this to `pullRadius()`.
   - `internal/physics/*` (body integration, soft speed cap, friction, collision impulse, the woodwork-hit bool) and `internal/geom/vec.go` math.
   - The interaction ORDER inside `Step`/`resolveInteractions`/`stepPenaltyPlay` and `ConfineBall`/`ConfinePlayer` restitution behaviour.
   - `internal/sim/field.go` `CheckGoal` goal-line crossing threshold (the `PitchLineWidth=3.0` clearance past `Min.X`/`Max.X`) and `goalMouthRange` — changing it changes WHEN a goal counts (an outcome), so it is feel. If you refactor `PitchLineWidth`, keep the value and keep `render.go` reading the same const.
   You MUST land a golden-replay characterization test FIRST (Workstream F.1) and treat ANY golden diff as a hard failure. Tuning the feel is NOT in scope; preserving it exactly is mandatory.

**(2) The AI is limited to EXACTLY what a human can do.** Both AI and human act ONLY through `Controller.Intent(view sim.View) sim.Intent` over real ticks, and observe ONLY through `sim.View`. The AI must not:
   - charge a shot/trap faster than a human (charge accumulates by `deltaTime`, driven only by `Intent.ShootHeld`/`Intent.Trap`);
   - read hidden state a human can't see (e.g. exact opponent `Velocity()`/internal `Heading()` (`moveHeading`)/`Possession()`/full tuning `Stats()`, the raw RNG `Seed()`, or any aggregated match-stats/recorder).
   This boundary must be ENFORCED at the type level and AUDITED by tests. Today it is NOT enforced (see B).

**(3) Preserve determinism.** No wall-clock (`time.Now`) and no unseeded RNG anywhere in `internal/sim` or `internal/control`. All variety is seed-driven (sim `rng.go` `coinToss`, control `noise.go` hash mixed with `view.Seed()`). Stats recording must be deterministic too (key off `m.Clock`/`m.Tick` only). Replays must be byte-identical. (Verified today: `math/rand` is seeded-only; no `time.Now` in sim/control.)

**(4) Keep the build green.** After every change: `go build ./...`, `go vet ./...`, `go test ./...`, and `go test -race ./...` must pass. The headless-server invariant must hold: `go list -deps ./cmd/server | grep ebiten` returns nothing. Existing tests stay green, including `sim/cancel_test.go` `TestCancelChargeSuppressesKick`, control `ai_test.go` `TestDeterminism`, and the AI sweeps (`TestQualitySweep`, `TestFlowQuality`, `TestGoalsScored`, `TestSkillTiers`, `TestKeeperSave`, …), plus `sim/aimassist_test.go`, `sim/touchboost_test.go`, and the many `*_internal_test.go` files.

---

## EXECUTION PLAYBOOK (how to work)

- Work in PHASES, fanning out sub-agents per workstream where independent, but SERIALIZE anything that touches `internal/sim` physics-adjacent files behind the golden-replay test.
- **Phase order is mandatory:** F.1 (golden replay test) → D-P0 items (config-tuning threading, render-globals, netcode race) → the `PlayerStats`→`PlayerTuning` rename (B.0) → A (stats feature) in parallel with B (boundary) → C (AI quality) → D-rest + E (UI/HUD) → F (full test build-out) → docs/CI.
- After EACH change set: run `go build ./...`, `go vet ./...`, `go test ./...`, `go test -race ./...`. If a golden trace diffs, STOP and revert — you altered the feel.
- Run AI-vs-AI simulations to validate behaviour and gather stats, INCLUDING the large 6-a-side map (use `config.LargeGeometry()`, formation build). Reuse the existing AI harness (`internal/control/ai_test.go` sweeps; `internal/control/passcompletion_test.go` `measureKicks` helper) across multiple seeds (>=6). Compare pass%, shots, on-target, goals before/after the boundary de-powering and record any regression.
- Make SCOPED commits per workstream/sub-item with clear messages. Branch off `main` (do not commit directly to `main`). Only commit/push when a workstream's acceptance criteria pass. End commit messages with the required `Co-Authored-By` trailer.
- NEVER alter physics feel. When in doubt, add a `if rec != nil` hook and a test, not a behaviour change.

---

## WORKSTREAM A — Match data, play-by-play recording & live stats

**Status: ENTIRELY TO DO.** There is no `record.go`, no `Recorder`/`MatchStats`/`Event`/`PassDir`/`PlayerStat`/`EventKind`/`EnableRecording`/`StatsSnapshot` symbol anywhere; `events.go` only carries the SOUND system. Build it fresh. **History note:** commit `e2f058d` ("Add recorder subsystem") once added one, but it was reverted/superseded — nothing remains today, so re-adding will not collide with any dead artifacts.

**Objective:** Add an opt-in, deterministic, WRITE-ONLY recorder in `internal/sim` that captures a chronological event log and folds it into per-player and per-team aggregates, hooked at existing authoritative sinks with ZERO physics-behaviour change, exposed via a clean read API, projected over netcode, and persisted as JSON per game. It must NOT be reachable through `sim.View`.

### A.1 Recorder subsystem (new file `internal/sim/record.go`)
Define (all with `json` struct tags):
- `type EventKind int` with consts: `EvTouch, EvPass, EvPassIncomplete, EvInterception, EvTackle, EvShot, EvShotOnTarget, EvSave, EvGoal, EvOwnGoal, EvClearance, EvOutOfPlay, EvKickoff, EvPenaltyKick`.
- `type PassDir int` with `PassForward, PassSideways, PassBackward`.
- `type Event struct { Tick uint64; Time float64; Kind EventKind; Player int; Team Side; Target int /* recipient/victim, -1 if none */; Pos geom.Vec; BallVel geom.Vec; Dir PassDir; Power float64; Flags uint8 }`.
- `type PlayerStat struct { PlayerID, Number int; Role Role; Side Side; Touches, PassesAttempted, PassesCompleted, PassesForward, PassesSideways, PassesBackward, KeyPasses, Assists, Interceptions, Tackles, PossessionWins, Saves, Shots, ShotsOnTarget, Goals, OwnGoals, Clearances int; PossessionSeconds, DistanceCovered float64; ThirdSeconds [3]float64 }` (thirds are per-team-relative: index 0 = own defensive third .. 2 = attacking third).
- `type TeamStat struct { Side Side; Name string; Goals, Shots, ShotsOnTarget, Passes, PassesCompleted, Interceptions, Saves int; PossessionSeconds float64; ThirdSeconds [3]float64 }` with method `PossessionPct(total float64) float64`.
- `type MatchStats struct { Players []PlayerStat; Teams []TeamStat; Events []Event }` (stable order keyed by `m.Players`/`m.Teams` order).
- `type Recorder struct { ... }` holding `Events []Event`, `Players map[int]*PlayerStat`, `Teams map[Side]*TeamStat`, and cross-event derivation state: `lastKickerID int`, `lastKickWasShot, lastKickOnTarget bool`, `lastKickTick uint64`, `lastShotTarget Side`, `prevPos map[int]geom.Vec`, plus a `drainIdx int` cursor for per-tick deltas.
- `func NewRecorder(m *Match) *Recorder` — pre-seeds `Players`/`Teams` from `m.Players`/`m.Teams` so every roster slot exists in deterministic order.
- All mutation helpers are methods on `*Recorder` and must be **nil-safe** (`func (r *Recorder) onTouch(...) { if r == nil { return } ... }`) so call sites need only `m.rec.onX(...)`.
- `func (r *Recorder) DrainNewEvents() []Event` — returns events appended since the last drain (mirrors `events.go` `DrainEvents`).

Do NOT use the name `Stats`/`MatchStat`/`PlayerStats` in a way that collides: `sim.PlayerStats` is the FROZEN per-player tuning block (until you rename it in B.0). Land A after B.0's rename frees the `Stats` name.

### A.2 Match wiring (`match.go`)
- Add unexported field `rec *Recorder` to `Match`.
- Add `func (m *Match) EnableRecording()` (sets `m.rec = NewRecorder(m)`), `func (m *Match) Recorder() *Recorder`, and `func (m *Match) Stats() MatchStats` (returns a deep COPY into stable-ordered slices — never leaks internal maps/pointers).
- Recording is OFF by default (nil recorder). Every hook is `if m.rec != nil { m.rec.onX(...) }` (or rely on nil-safe receivers). Disabled recording must be byte-identical to today.

### A.3 Hooks (at EXISTING authoritative sinks — verify exact lines in source)
The current `Step` pipeline order is: `applyIntent` → `advancePossessionBuilder` → the `updatePossession` loop → `updateBallPossessor` → `advanceTeamPossession` → `resolveInteractions(deltaTime)` → the dribble-touch loop `recordTouch(p, TouchDribble)` → the `if p.WantsKick { if shoot(p, m.Ball) { recordTouch(p, TouchKick) } }` loop → `enforceZoneRules` → `advanceRules`.
1. **Touches / passes / interceptions / tackles** — in `recordTouch` (`scoring.go`). Mirror its existing distinct-toucher collapse: count one `Touches` per genuinely NEW distinct toucher. On a transition where the previous distinct touch was `TouchKick`: same team next toucher → completed `Pass` (increment passer `PassesCompleted` + classify direction); opponent next toucher → previous kicker `PassesAttempted` (not Completed) + opponent `Interceptions`. When the previous touch was `TouchDribble` and an opponent takes it → `Tackle`/`PossessionWin`. A key pass = a completed pass whose receiver's next event is a Shot.
2. **Shots / shots-on-target** — in the `Step` kick/shoot loop (`match.go`, the `if p.WantsKick { if shoot(p, m.Ball) {...} }` block) and its mirror in `stepPenaltyPlay` (`penalties.go`). On a successful `shoot()`, call `m.rec.onKick(p, m.Ball, m)`: always store kicker + from-position (for pass derivation); count a `Shot` only when goal-directed; test shot-on-target by ray-casting the post-kick `ball.Velocity` from `ball.Position` against the attacking goal mouth segment (`f.GoalOn(attacking.Opponent()).Mouth`, reuse `goalMouthRange`/`CheckGoal` geometry — READ-ONLY ray, no mutation). Bound the ray by a max distance derived from ball friction so a straight ray doesn't over-count long shots that decelerate and drop short. Set the latch fields for save derivation.
3. **Saves / woodwork** — in `resolveInteractions` (`match.go`) and the penalty mirror. When `handleBallToPlayerInteraction` reports a touch by a defending player while `lastKickWasShot && lastKickOnTarget` and the ball has NOT yet crossed (before `CheckGoal` fires), and the toucher is `RoleGoalkeeper` OR inside its own goal-area box → record `EvSave`. Capture the existing `physics.Collide` return on post/net (there are SIX `physics.Collide` calls in `resolveInteractions` — at the ball-vs-obstacle/post/net and player-vs-obstacle/post/net sites) to flag a woodwork hit on an on-target shot (READ the bool already returned; do NOT change the call).
4. **Goals / assists / own-goals / deflections** — in `resolveGoal` (`scoring.go`), AFTER it computes the `ScoreEvent`. Call `m.rec.onGoal(ev, m)` and emit `EvGoal`/`EvOwnGoal` reading `ev.Scorer/Assist/OwnGoal/Deflected` — DO NOT re-derive (single source of truth shared with the HUD `goalText`).
5. **Per-tick possession / distance / thirds** — add `m.rec.sample(m, deltaTime)` in `Step` right after `advanceTeamPossession`/`updateBallPossessor` (uses `m.ballCarrier()` from `zonerule.go`): add `deltaTime` to the firm carrier's and their team's `PossessionSeconds` (loose-ball ticks count toward neither); for every player add `geom.Dist(p.Position, prevPos[id])` to `DistanceCovered` then store `prevPos[id]`; classify ball X into per-team-relative thirds and add `deltaTime` to `ThirdSeconds`. Reads positions only; never writes them.
6. **Kickoff / clearance** — in `resetKickoff` (`match.go`) call `m.rec.onKickoff(m)` to emit `EvKickoff` and reset pass-derivation latches (so a pass is never attributed across a kickoff, mirroring the existing `touchHistory` clear). Classify a defender's `TouchKick` that sends the ball from the defending third past midfield as a `Clearance` (derive in `onKick`).
7. **Penalty kicks** — in `recordKick(scored bool)` (`penalties.go`) emit `EvPenaltyKick` with made/missed flag + per-player penalty tallies.

### A.4 Persistence (`internal/sim/record_json.go`, or a thin `internal/persist` package if you prefer sim to stay serialization-free)
- `type MatchRecord struct { Schema int; Seed int64; Geometry config.Geometry; Ruleset config.Ruleset; Teams [2]TeamInfo; Players []PlayerInfo; FinalScore [2]int; Winner Side; DurationSeconds float64; Events []Event; PlayerStats []PlayerStat; TeamStats []TeamStat }` with `TeamInfo{Name,Color,Side}` and `PlayerInfo{ID,Number,Role,Side}`.
- `func (r *Recorder) MatchRecord(m *Match) MatchRecord` and `func (mr MatchRecord) WriteJSON(w io.Writer) error` (encoding/json, struct tags). `json.Unmarshal` must round-trip.
- Output filename derived from `Seed` (+ a caller-supplied id), **never `time.Now`**, to stay reproducible.

### A.5 Transport & live HUD plumbing (coordinate with D & E)
- Add `Stats StatsSnapshot` (flattened per-player/per-team counters for the live HUD) and `Events []sim.Event` (THIS TICK's delta only, via `DrainNewEvents`) to `netcode.Snapshot`; project in `SnapshotOf` reading `m.Recorder()` when non-nil. Do NOT resend the full event log every tick.
- `menu.App` calls `EnableRecording()` in `startMatch` and writes a `MatchRecord` JSON on transition to `StateResult`. Headless server may enable + persist on Finished. Optional `-stats-out path` flag in `config.ParseGame` (default off).

**ACCEPTANCE CRITERIA (A):**
- With recording disabled, `Match.Step` is byte-identical to pre-change (golden replay test passes) — zero feel change.
- A scripted seeded match replayed twice with recording ENABLED yields `reflect.DeepEqual` `MatchStats` and identical `Events`; `grep` shows no `time.`/`rand.` in the new files.
- Each distinct-toucher contact increments exactly one `Touches` (repeat same-player contact collapses).
- Forward/square/backward completed passes produce `PassForward/PassSideways/PassBackward` via team attack axis (+X for `SideLeft`, -X for `SideRight`); an opponent intercepting a `TouchKick` increments the intended passer's `PassesAttempted` (not Completed) and the opponent's `Interceptions`.
- A kick whose post-kick velocity ray crosses the goal mouth between the posts counts `ShotsOnTarget`; a wide kick counts `Shots` only — both against the same `goalMouthRange`/`CheckGoal` geometry.
- A defending keeper/in-box defender touching an on-target shot before `CheckGoal` fires increments `Saves` + emits `EvSave`; an uncontested goal records no save; own-goals/deflections read `resolveGoal` and are not double-counted.
- Possession seconds sum correctly (loose-ball excluded); `PossessionPct` derived at read time. Distance for a player at known speed over N ticks equals `speed*dt*N` within float tolerance.
- `MatchRecord.WriteJSON` emits valid JSON with seed/rosters/score/full chronological events/aggregates; round-trips via `json.Unmarshal`; filename derived from `Seed`.
- **Boundary:** a test asserts `sim.View`/`PlayerView` expose NO recorder/stats accessor; the recorder is reachable ONLY via `Match.Recorder()`/`Stats()`.

---

## WORKSTREAM B — AI <-> sim API boundary & capability limiting

**Status: MOSTLY TO DO (one facet already landed).** `view.go` still exposes `Seed()` (l.214) and, for ALL players, `Velocity()`/`Heading()`/`Possession()`/`Stats()`/`ShootCharge()`/`TrapCharge()`. There is no `SelfView`/`ObservedView` split, no `NoiseSalt`, and `PlayerStats` is not renamed. The AI still reads opponent/teammate hidden state. **ALREADY DONE:** `ai.go` `enforceAbilityExclusivity` now clamps the AI to one ability at a time (`Trap > Push > Shoot`, cancel-on-takeover), matching the human's mutually-exclusive mouse buttons — see B.6; keep and TEST it. The rest of B (View narrowing + hidden-state migration) remains.

**Objective:** Keep the single `Controller.Intent(view sim.View)` seam and make it provably human-equivalent by closing three gaps: (1) narrow the observation surface so the AI sees no more about non-self players than a human's HUD shows; (2) pin charge/trap tick-rate limiting with tests; (3) fix the false `CancelCharge` invariant. No physics/feel change — the View split is a pure method-set narrowing; charge math is untouched.

### B.0 Rename first (mechanical, guarded by existing control tests)
Rename `sim.PlayerStats` → `sim.PlayerTuning`, `PlayerView.Stats()` → `PlayerView.Tuning()`, `StatsForRole` → `TuningForRole`, `DefaultStats` → `DefaultTuning` (avoid colliding with `config.DefaultTuning` — namespace it, e.g. `DefaultPlayerTuning`). Update `stats.go`, `view.go`, `entity.go` (`Player.Stats` field → `Tuning`), `roles.go`, and ALL `internal/control` callers (every `Stats()` site listed in B.1, plus `abilities.go` `Stats().ShootDirection`/`MinShootFactor` and `onball.go` `Stats().MinShootFactor`). This frees the "Stats" name for Workstream A. Land as ONE commit; control tests stay green.

### B.1 Observation audit & View split (`sim/view.go`)
Ground truth for "what a human perceives" is the rendered `Snapshot`: `netcode.EntityState` exposes only `{Kind, Position, Facing, Radius, Color, Number, ShootCharge, TrapCharge}`; `render.go` draws facing + both charge gauges for ALL players. NOT rendered anywhere: player `Velocity`, internal `moveHeading` (`PlayerView.Heading()`), `Possession()`, full tuning `Stats()`/`Tuning()`. Yet today the View exposes all four for every player and the AI READS opponent/teammate hidden state at these confirmed sites:
- `perception.go:57` — `view.Seed()` (into the noise seed).
- `perception.go:150-151` — `o.Stats().MaxSpeed` and `o.Velocity()` (`openDuration`).
- `offball.go:125` — `o.Stats().MaxSpeed`, `o.Stats().TurnRate`, `o.Heading()` (`interceptTime` for opponents).
- `keeper.go:131` — `o.Stats().MaxSpeed`, `o.Stats().TurnRate`, `o.Heading()` (`keeperShouldSweep`).
- `teamplan.go:71` — `q.Stats().MaxSpeed`, `q.Stats().TurnRate`, `q.Heading()` (`interceptCost`, used for BOTH teams incl. opponents via `electPresser(p.opponents)`).
- `predict.go:108` — `o.Stats().MaxSpeed` (`laneSafe`; note: MaxSpeed only, no TurnRate here).
- `onball.go:343` — `mate.Velocity()` (`leadPoint`); `onball.go:353` — `q.Stats().MaxSpeed` (`timeToPoint`).

Fix:
- Introduce `type ObservedView interface { ID() int; Number() int; Role() Role; Side() Side; Position() geom.Vec; Facing() geom.Vec; Radius() float64; ShootCharge() float64; TrapCharge() float64; Same(ObservedView) bool; SameTeam(ObservedView) bool; AngleToFacing(geom.Vec) float64; BallAngleToFacing(BallView) float64 }` — EXACTLY the human-perceivable fields. (Charges stay because the HUD renders gauges for all players.)
- Introduce `type SelfView interface { ObservedView; Velocity() geom.Vec; Heading() geom.Vec; Possession() float64; Tuning() PlayerTuning; HomePosition() geom.Vec }`.
- Change `View`: `Me(id int) (SelfView, bool)`; `Carrier() (ObservedView, bool)`; `Teammates(of ObservedView) []ObservedView`; `Squad(of ObservedView) []ObservedView`; `Opponents(of ObservedView) []ObservedView`. `matchView.Me` returns `selfView{p}`; the rest return `observedView{p}`. `unwrapPlayer` must type-switch on BOTH wrapper types (unit-test this).
- Remove `View.Seed() int64`; add `View.NoiseSalt(id int) int64` returning a deterministic `hash(seed, id)` so AI variety survives without leaking the raw seed. (Keep the `Match.Seed` field; only remove the View method.)

### B.2 Migrate AI reads (`internal/control`)
The ONLY behavioural change to the AI, a deliberate de-powering toward human capability:
- Add `aiTuning.assumedOppSpeed float64` and `assumedOppTurn float64` (set in `defaultAITuning()` to the current shared `MaxSpeed`/`TurnRate` so the nominal case is closest to today).
- Replace every opponent/teammate `o.Velocity()`, `o.Heading()`, `o.Stats()/.Tuning()` read (the seven sites above) with `assumedOppSpeed`/`assumedOppTurn` constants and a heading that does NOT read an opponent's committed steering (use a vector toward the ball or the rate-unaware variant). SELF reads via `Me` (`p.me.Velocity()/.Heading()/.Tuning()/.ShootCharge()/.Possession()`) remain legal.
- `perception.go` consumes `view.NoiseSalt(id)` instead of `view.Seed()` (the seed is mixed into `noise`/`personality`/`gaussian`/keeper-misread/`avoid` channels — route the salt through `perceive`).

### B.3 CancelCharge invariant fix
`intent.go:15-18`, `match.go:217` (and the doc in `human.go`) all assert "the AI never sets CancelCharge". This is FALSE: `abilities.go:191` (`shootAt` overtime branch), `push.go:84` (the middle-click jab abort), AND the new `ai.go` `enforceAbilityExclusivity` (a trap/push taking over a live charge) all set `in.CancelCharge = true`. The action (abort a stuck charge) IS human-reachable (right-click rising edge — `human.go` already sets it). Keep the AI behaviour and CORRECT the comments to: "CancelCharge is a human-reachable signal (right-click rising edge); the AI may also use it to abort a stuck charge or when a trap/push overrides a charge, exactly as a human can." Expand the `intent.go` doc to enumerate the human-input → field mapping (WASD→Move/Throttle, cursor→Aim + `AimFromCursor`, LMB→ShootHeld, RMB→Trap, RMB rising edge→CancelCharge, MMB→Push).

### B.4 Charge/trap tick-limiting (already correct — pin it)
No code change: `applyIntent` accumulates `shootCharge`/`trapCharge` by `deltaTime`, capped at `shootChargeMax=0.75`/`trapChargeTime=1.25`, driven only by Intent. The AI's `shootAt` only HOLDS `ShootHeld` across ticks. Pin with tests (F).

### B.5 Boundary test suite (`internal/control/boundary_test.go`, `internal/sim/chargerate_test.go`)
- Assert a non-self handle (from `Opponents`/`Teammates`/`Carrier`) does NOT satisfy an interface exposing `Velocity()/Heading()/Possession()/Tuning()` (runtime type-assertion must fail).
- Assert every Intent emitted over a scripted multi-second match is human-reachable: `Throttle in [0,1]` and finite, `Move`/`Aim` finite, and at most ONE of {`Trap`, `Push`, fire-this-tick} active (ability exclusivity, B.6).
- Assert `shootCharge` after holding `ShootHeld` for k ticks of `dt` equals `min(k*dt, shootChargeMax)` (=0.75) — the AI cannot reach full charge faster than a human.
- Assert the AI's overtime cancel (and the push abort) and a human right-click cancel produce the same sim result (charge dropped, no kick on release) — extend `sim/cancel_test.go`.
- Determinism: two AIs from the same seed + scripted view emit byte-identical Intent sequences — extend control `TestDeterminism` (`ai_test.go:539`).

### B.6 Ability exclusivity (already implemented — keep & pin it)
`ai.go` `enforceAbilityExclusivity` already enforces that the AI asserts at most ONE of `Trap > Push > Shoot` per tick (cancelling a live charge when a higher-priority ability takes over), mirroring `input.Human` (three mutually-exclusive mouse buttons). Do NOT regress this. Verify `input.Human` applies the SAME precedence so AI and human are identical, and PIN it with a test (see B.5): over a scripted match the AI never emits an Intent with more than one of {`Trap`, `Push`, `ShootHeld`+fire} active, and a trap/push over a charge drops the charge (no kick) rather than firing.

**ACCEPTANCE CRITERIA (B):**
- The AI never emits an Intent with more than one of {`Trap`, `Push`, fire-this-tick} active; a trap/push over a live charge cancels it (no kick) — same precedence as `input.Human` (B.6 test passes).
- `grep` across `internal/control` shows NO `.Velocity()/.Heading()/.Possession()/.Tuning()/.Stats()` call on any opponent/teammate handle (only on the `SelfView` from `Me`); opponent motion estimates use `assumedOppSpeed`/`assumedOppTurn`.
- `View` has no `Seed()`; variety derives from `NoiseSalt(id)` (pure hash, no raw seed leak).
- The CancelCharge comments are corrected at all three sites; the cancel-equivalence test passes.
- All boundary/charge-rate/determinism tests pass; existing suites stay green; headless deps check still passes.
- `ObservedView`'s field set is documented as matching `netcode.Snapshot`/`EntityState` + rendered gauges, with a cross-reference comment in both `sim/view.go` and `netcode/protocol.go`.

---

## WORKSTREAM C — AI quality improvements (still bounded by the API)

**Objective:** Improve clarity, testability, and tuning hygiene of the AI WITHOUT exceeding the human capability boundary from B and WITHOUT changing the sim. All changes act only through Intent over real ticks.

Tasks (file-referenced — re-verify lines):
- Delete the dead `aiTuning` fields — confirmed truly UNREAD outside `tuning.go`: `shootGap` (value 3), `turnTrapSettled` (value 0.5), `trapApproachFactor` (value 0.7, comment marks it "(reserved)"). (Note `cornerInset` is NO LONGER dead — it is now read at `onball.go:204` — so do NOT remove it.) Either delete the three dead fields or wire them to their intended use. No dead tuning surface.
- Lift inline magic numbers into named `aiTuning` fields: `onball.go:93` `const stick = 0.15`; the `bestPass` score weights at `onball.go:285` (`advance*0.009`, `space*0.006`, `safety*0.5`, `openDur*0.4`) and the recycle clamp `onball.go:289` (`1.12`); `abilities.go:35` `aimProjectDist=1000`, `abilities.go:132` `aimKeepingBall` turn cap `clampFloat(1-ballAng/arc, 0.3, 1)`, `abilities.go:203` `th *= 0.6`; `keeper.go` misread blend `0.7`/`0.3` and `/30`,`/50` quantization; `steering.go`/`offball.go` avoid/separation magic factors. One place to tune. (Note: the old prompt's "throttle/blend 0.6/0.3" pair is stale — current `shootAt` uses only `th *= 0.6`.)
- Split `onball.go` (now 535 lines): separate pure SCORING (functions returning a typed `[]passCandidate`/action-score with named weighted terms) from EXECUTION (turning a chosen action into an Intent). `bestPass(p perception) (geom.Vec, sim.PlayerView, float64)` becomes unit-testable. **Pure refactor — behaviour identical** (verify via AI harness pass%/shots/goals across >=6 seeds before/after, 6-a-side included).
- Harden nil/empty paths: `nearest()` returns `(nil, +Inf)` when the list is empty; guard `p.nearestOppToMe` usage in `bestDribble`/`shield`/`avoid`; add a defensive early return in `AI.Intent` for a foreign/nil View. Removes latent panics, no behaviour change with a normal roster.
- Document and ideally enforce the ID→formation-slot convention (`formation.go`/`teamplan.go`/`zones.go`), or derive back-to-front ordering from `Role`/`HomePosition` instead of relying on PlayerID order.
- Replace the obscure `in.Move == (sim.Intent{}).Move` zero-vector check (`ai.go:141` `applyMoveJitter`) with a clear `geom.Vec{}` comparison.

**ACCEPTANCE CRITERIA (C):**
- No dead `aiTuning` fields (`shootGap`/`turnTrapSettled`/`trapApproachFactor` gone or wired); all listed magic numbers live in `aiTuning`.
- `onball.go` scoring is pure functions returning typed candidates; behaviour unchanged (AI harness metrics within tolerance across >=6 seeds, 6-a-side included).
- No panic with empty teammate/opponent lists; defensive early return for foreign View.
- All existing + new control tests pass; determinism preserved.

---

## WORKSTREAM D — API, code structure & production hardening

**Objective:** Production scaffolding and correctness landmines, all behaviour-preserving and guarded by the golden replay test.

### D-P0 (correctness/boundary landmines — do these right after F.1)
- **Config-tuning no-op trap:** `config.Tuning` is stamped onto `Match.Tuning` (`match.go:97`) but only `BallFriction` is read (via `matchView.BallFriction()` at `view.go:215`). `match.go` hard-codes `obstacleRestitution=0.5`/`netRestitution=0.2`/`ballWallRestitution=0.90`/`playerWallRestitution=0.50`, and `NewBall` uses friction `-0.3`/mass `1.5` (with radius literal `7.5` at the call sites). Thread `m.Tuning.*` into every site — the SIX `physics.Collide` calls in `resolveInteractions` (`match.go:724,728,731,753,757,760`; two more in `stepPenaltyPlay`, `penalties.go:329,332`), `field.go` `ConfineBall`/`ConfinePlayer`, `zonerule.go`, `penalties.go`, and `NewBall(center, cfg.Tuning.BallRadius)` with friction/mass from Tuning. `config.DefaultTuning()` is BYTE-EQUAL to the sim consts (`BallRadius 7.5`, `BallFriction -0.3`, `BallMass 1.5`, `BallWallRestitution 0.90`, `PlayerWallRestitution 0.50`, `ObstacleRestitution 0.5`, `NetRestitution 0.2`), so the golden trace must NOT change. Make `View.BallFriction()` read the ACTUAL ball body friction (`m.Ball.Body` friction) so it can't drift from the config. Land incrementally; any golden diff = revert.
- **Render transform globals:** Replace the package-global transform (`view`, `worldW`, `worldH`, `camActive`, `camCenter`, `camZoom` in `render.go:93-107`) with a `type Viewport struct { worldW, worldH float64; camActive bool; camCenter geom.Vec; camZoom float64 }` plus `(Viewport) ScreenToWorld(x,y int) geom.Vec` (currently a package func at `render.go:209`); `render.Frame` returns the `Viewport`. `input.Human` and the menu cursor mapping take the `Viewport` from the App instead of the package global. Presentational only. Account for the extra debug HUD (`ShowPossessionBars`/`drawPossessionBarsAll`/`drawTrapAura`/`drawShootCharge`) that also reads/writes the transform.
- **Netcode `Client.Send` data race:** `Client.Send` (`client.go:50`) calls `c.enc.Encode` and `Close` (`client.go:61`) calls `c.conn.Close` with NO mutex (the existing `c.mu` guards only `latest`/`hasSnap`). Guard `Send` with a send mutex (or a single-writer goroutine fed by a buffered latest-Intent channel) so `Send` and `Close` can't touch the gob encoder/conn concurrently. Add a `-race` CI target.

### D-rest (P1/P2)
- **Netcode protocol robustness:** Add `const ProtoVersion = 1`, a `type Envelope struct { Kind MsgKind; Hello *Hello; Snapshot *Snapshot; Stats *StatsSnapshot }` tagged union, `type Hello struct { ProtoVersion int; AssignedPlayerID int }`. On accept the server sends `Hello` (client learns which entity it controls — today `cmd/client/main.go` sends `g.human.Intent(nil)` for whatever slot the server assigned, with no way to know which); reject version mismatch. Add per-conn `SetWriteDeadline`; move the broadcast `Encode` OFF the tick goroutine (currently runs there after unlocking, `server.go` `tickLoop`) into a per-conn sender goroutine fed by a latest-snapshot channel that drops stale frames. Expire stale intents (stamp arrival tick; fall back to neutral Intent after N ticks). Add TCP keepalive + read deadline. Log `Encode` failures at Warn (today they are dropped silently). Keep `Step` single-threaded/authoritative (untouched).
- **View boundary narrowing (read-side):** Done structurally in B; ensure `View.Seed()` removal and self-only velocity/heading/possession/tuning exposure are reflected here and documented as human-equivalent.
- **Error handling/validation:** `menu.Settings.Config()` must surface the build error (store `lastError` shown on the setup screen + log at Warn) instead of silently falling back to `config.Default()` — STILL not done (the menu Start button IS now gated on `Settings.Validate()`, but `Config()` itself still swallows). (DONE already: `MatchSetup.Validate` is now the SINGLE validator and calls `Geometry.Validate()`/`Normalize()`.) Centralize the `EvictGrace` default as a `config.DefaultEvictGrace` const applied in `MatchSetup.Ruleset` (it is a magic `0.5` literal in `flags.go` today; `DefaultMatchSetup` leaves it 0). Break difficulty duplication: add `control.ValidSkill(string) bool`/`control.SkillNames() []string` (today only `control.SkillFromString` exists at `tuning.go:259`) and validate difficulty in `cmd` (which imports control); remove `config.validDifficulty` (`flags.go:182`). Reconcile the THREE/FOUR-way difficulty-name drift: `SkillFromString` accepts `easy/normal/medium/hard/pro/impossible/perfect` (+`""`/`default`), `config.validDifficulty` hand-copies that set, `menu.difficultyPresets` offers only `{easy, normal, hard}` (omits `impossible`), and the flag help says only "easy, normal, hard, or impossible". Reconcile the camera drift too: `CameraModeFromName` and `-camera` accept `follow`(=ball)/`active`(=player) aliases but the help text says only "ball, player, or fit".
- **CLI scaffolding:** Extract the triplicated `code(err)` exit-mapper (`cmd/client/main.go:123`, `cmd/server/main.go:30`, `cmd/game/main.go:49`) + the `signal.NotifyContext` scaffold into a new `internal/cliutil` package (`func Code(err error, prefix string, stderr io.Writer) int`), used by all three `cmd/*/main.go`.
- **Build/CI:** Add a `Makefile` (`build`, `test`, `test-race`, `vet`, `headless` = `go list -deps ./cmd/server | grep -q ebiten && exit 1 || exit 0`) and `.github/workflows/ci.yml` running vet + test + test-race + headless guard + the golden replay test on push/PR. Pin `go.mod`: bump the `go` directive and add a `toolchain` line (none exists today) matching the installed Go. Document the `phootball` (module) vs `haxball-clone` (dir) name divergence.
- **Docs:** Update `README.md` flag table (add `-goalarea-box-max`, mark `-gk-box-max` deprecated), document the stats feature + JSON schema, the build/CI story, and the headless invariant. Note that `cmd/game`'s default lobby path seeds only TeamSize/Seed/Difficulty into `menu.DefaultSettings` and ignores CLI geometry/rules unless a fast-path flag (`-solo`/`-duo`/`-ai-both`) is set.
- **Dead API:** Document or remove unused `physics.ReflectInside`/`ClampInside`, or have `Field` confine helpers delegate to them. Do not change confine behaviour.

**ACCEPTANCE CRITERIA (D):**
- Threading `config.Tuning` changes NO golden-replay bytes (config now authoritative AND feel unchanged); `View.BallFriction()` reads the real ball-body friction.
- Render is reentrant (no package-global transform); `Viewport.ScreenToWorld` round-trips in a test.
- `go test -race ./...` clean; the `Client.Send` race is gone.
- Server sends `Hello{ProtoVersion, AssignedPlayerID}`; version-mismatched client rejected; a slow client no longer back-pressures the tick loop; a silent client's stale intent expires; `Encode` failures logged at Warn.
- `Settings.Config()` surfaces errors; single validator; `DefaultEvictGrace` centralizes evict behaviour; `config.validDifficulty` removed in favor of `control.ValidSkill`; difficulty/camera name drift reconciled.
- `internal/cliutil` removes the three-way duplication; Makefile + CI workflow exist and are green; `go.mod` toolchain matches installed Go; headless deps check passes.

---

## WORKSTREAM E — Menus, UI/UX & rendering (incl. in-match stats HUD)

**Objective:** Surface live stats and harden the presentation layer, with no feel/determinism impact (all presentational, client-only, no wall-clock/RNG into `Step`).

**Status: LARGELY DONE by the menu/UI rework.** DONE: the `Theme`/layout module (`menu/theme.go`), the sized scalable-vector-text API (`textSized`/`TextS`/`MeasureUI`), the unified HUD (`HUDModel` + `DrawHUD`, built from `hudFromMatch`/`HUDFromSnapshot`), per-team control + tabbed Match Setup, goal-overlay/stage-card FX, and the full Result screen (`screenResult`, with a placeholder STATS section). STILL TODO: the items below marked TODO.

Tasks:
- **PARTIAL** — the HUD is unified (`DrawHUD`/`HUDModel`), but the THREE full-match draw bodies still exist (`render.Frame`, the now-DEAD `render.Match`, the `cmd/client/main.go` reassembly) — collapse them into ONE full-match renderer and delete the dead `render.Match`/legacy `Scoreboard*`/`CenterBanner`/`ShootoutPanel`.
- **TODO (blocked on A)** — Add `render.StatsPanel(...)` + a `Tab` toggle in `menu/app.go` `updatePlaying`. `StatsModel` is a plain struct built from `match.Stats()` locally or `Snapshot.Stats` on the client — identical numbers both ways. (The Result screen already has a scaffolded STATS placeholder; this is the live in-match panel, which needs the recorder from A.)
- Single source of truth for keybindings + control hints: a controls table consumed by both `app.go` input handling and the hint strings (eliminate the drift between `render.Scoreboard`, `render.ScoreboardWithClock`, and the `menu/app.go` `screenSettings` CONTROLS list). Include the newer MMB-push and RMB-cancel bindings.
- Harden App state: enforce `match != nil` invariant for `Playing`/`Paused`/`Result` (guard or a small `goto(state)` transition helper managing `prevState` as a STACK — today it is a single field); ensure Quit-to-Menu clears stale `a.controllers` (today it only nils `a.match`).
- **DONE** — `Theme`/layout module is in `menu/theme.go` (palette + type scale + `colLayout`), consumed across `app.go`/`settings.go`/`ui.go`.
- Move shared helpers (`clampF`/`clampInt`/`absF`/`dir` from `menu`, `itoa` from `render`) into one util (or reuse `geom`); replace ad-hoc text alignment (`len(s)*3`, e.g. `render.go:245,738,748`) with measured widths via a single text-face abstraction.
- **PARTIAL** — the Result screen (`screenResult` + `menu/result.go`) is DONE (scoreline, team chips, winner/AET/penalties tag, goal-by-goal timeline, shootout dots, trophy, placeholder STATS). STILL TODO: persist the `MatchRecord` JSON on transition to `StateResult` (needs A) and fill the real stat sheet.
- Keep all stats on the presentational side — do NOT feed any stats/recorder back into `sim.View`.

**ACCEPTANCE CRITERIA (E):**
- One full-match renderer + one `drawHUD`; `cmd/client` builds its HUD/stats model from the snapshot.
- `Tab` toggles a live stats HUD in a local match; the network client renders identical numbers from `Snapshot.Stats`.
- Single keybindings/control-hints source (no drift); App enforces `match != nil`; `Settings.Config()` errors are visible.
- Result screen shows the final stat sheet and a JSON file is written (filename derived from Seed).
- Pure UI logic (camera `clampHalf`/`clampZoom`/`smoothAlpha`, `formatClock`, `itoa`, `Viewport.ScreenToWorld`, settings `cycle`/`clamp*`/`seedSizesFromField`) is unit-tested. No stats exposed via `View`.

---

## WORKSTREAM F — Testing & verification strategy

**Objective:** A layered test foundation that pins the frozen feel, determinism, the boundary, and the new stats — all deterministic, fast, no wall-clock/RNG.

Existing tests to BUILD ON (do not duplicate): `internal/sim/cancel_test.go` (`TestCancelChargeSuppressesKick` + possession/team-charge cases), `sim/aimassist_test.go`, `sim/touchboost_test.go` (large team-charge/touch-quality suite incl. `TestTeamChargeThroughStep`, `TestMaxShotNotCapturedByDebuffedOpponent`), and the `*_internal_test.go` set (`sim/shoot_internal_test.go`, `possessionpull_internal_test.go`, `boostdrain_internal_test.go`, `contactpush_internal_test.go`, `cornerconfine_internal_test.go`, `trapaura_internal_test.go`, `zonerule_internal_test.go`, `penalties_internal_test.go`); `control/ai_test.go` (`TestQualitySweep`/`TestFlowQuality`/`TestSkillTiers`/`TestGoalsScored`/`TestDeterminism`/`TestKeeperSave`/…), `control/passcompletion_test.go` (with the `measureKicks` helper), `control/pass_internal_test.go`, `control/aim_internal_test.go`, `control/dribble_internal_test.go`, `control/turnrate_internal_test.go`, `control/keeperwall_internal_test.go`, `control/keeperfacing_internal_test.go`, `control/push_internal_test.go` (the ability rename — was `poke_internal_test.go`; `TestPushClearsGeometry`/`TestPushShotOnRange`/`TestKeeperPushesAwayUnderPressure`), `control/receive_internal_test.go`; and **`internal/config`** now has `combos_test.go`/`phase2_test.go`. NO golden replay test and NO `internal/netcode`/`render`/`logging`/`menu` tests exist yet.

### F.1 — DO THIS FIRST: golden deterministic replay (`internal/sim/replay_test.go`)
Build a fixed-seed match (`config.Default()` via `BuildMatchFromConfig`), drive it with a scripted `[]map[int]Intent` for ~3600 ticks, and assert a recorded trace (per-N-tick ball `Position`/`Velocity`, scores, `LastGoal` attribution) byte-matches a golden file `internal/sim/testdata/replay_default.golden`. Provide a `-update` flag (via `flag.Bool`) to regenerate. This is the safety net for ALL refactors — run it after every change to physics-adjacent code.

### F.2 — Pure unit tests for the currently-untested packages
- `internal/geom` (vec math), `internal/physics` (circle-circle/circle-segment impulse, soft speed cap, static immovability characterization, the woodwork-hit bool from `Collide`).
- `internal/config` (`ParseGame`/`ParseServer`/`ParseClient` range/`-version`/`-h`/exit-code sentinels; `DefaultMatchSetup().Build()` deep-equals `Default()`; preset `Min`/`Max`/`Center` + `Normalize` idempotency; README example round-trips; `LargeGeometry()`).
- `internal/netcode` (table-test `sanitizeIntent` NaN/Inf/throttle clamp; `freeSlot` assignment+reuse; `Snapshot`/`Envelope` gob round-trip asserting every `SnapshotOf` field maps; `Run(ctx)` cancellation; handshake version mismatch).
- `internal/render`+`camera` (`clampHalf`/`clampZoom`/`smoothAlpha`/`Viewport.ScreenToWorld` round-trip, `formatClock`/`itoa`), and `internal/logging` (`ParseLevel`/`ParseFormat`).

### F.3 — Stat-accuracy scenarios (`internal/sim/stats_test.go`)
Script known fixed-seed scenarios — a clean forward pass A→B, an opponent interception, a keeper save of an on-target shot, a deflected goal, an own goal — and assert `MatchStats` exactly equals hand-computed `PlayerStat`/`TeamStat` lines. Cross-check the recorder's pass-completion% against the independent control harness (`measureKicks`/`passcompletion_test.go`) on the same seeds to catch double-counting.

### F.4 — Boundary & determinism (Workstream B tests)
The B.5 suite (`control/boundary_test.go`, `sim/chargerate_test.go`) PLUS the stats boundary test (`internal/sim/boundary_test.go`) asserting no `View`/`PlayerView`/`SelfView`/`ObservedView` method returns `MatchStats`/`PlayerStat`/`Event`, and that a non-self handle cannot be type-asserted to `SelfView`.

### F.5 — AI-vs-AI behavioural harness
Reuse/extend the `internal/control/ai_test.go` sweeps across >=6 seeds, INCLUDING a 6-a-side `config.LargeGeometry()` formation match (`BuildMatch`). Record pass%, shots, shots-on-target, goals, possession both before and after the B de-powering and the C `onball.go` refactor; treat large unexplained regressions as failures to investigate. Use these runs to validate the stats feature produces sane football numbers.

**ACCEPTANCE CRITERIA (F):**
- `go test ./...` and `go test -race ./...` pass; `go vet ./...` clean; headless deps check passes.
- The golden replay test is byte-identical before and after every behaviour-preserving refactor.
- Every previously-untested package (`geom`, `physics`, `config`, `netcode`, `render`/`camera`, `logging`) has meaningful tests.
- Stat-accuracy scenarios match hand-computed expectations and agree with the independent control harness; boundary tests pin constraints (2) and (3).
- The AI-vs-AI harness (incl. 6-a-side) runs deterministically and the recorded stats are football-plausible.

---

## DELIVERABLES & DONE CHECKLIST

- [ ] `internal/sim/record.go` + `record_json.go` (or `internal/persist`): deterministic, write-only recorder; `Event`/`PassDir`/`PlayerStat`/`TeamStat`/`MatchStats`/`Recorder`/`MatchRecord`; `Match.EnableRecording()`/`Recorder()`/`Stats()`; hooks at `recordTouch`, the kick/`shoot` loop, `resolveInteractions` (incl. the 6 `Collide` woodwork bools), `resolveGoal`, the possession sampler, `resetKickoff`, `recordKick` — all `if m.rec != nil`, default off, byte-identical when disabled.
- [ ] Full stat list recorded: touches, passes (forward/side/back, attempted/completed/incomplete), key passes, assists, interceptions, tackles/possession-wins, saves, shots, shots-on-target, goals, own-goals, clearances, possession seconds/%, distance, time-in-thirds, penalty kicks — per player and per team — plus a chronological event log.
- [ ] JSON persistence per game, filename derived from `Seed`; round-trips; carries seed/rosters/ruleset/geometry/score/events/aggregates.
- [ ] Netcode `Snapshot` carries live `Stats` + per-tick `Events` delta; live stats HUD toggled with `Tab`; identical numbers local vs network; Result screen stat sheet.
- [ ] AI boundary: `SelfView`/`ObservedView` split; opponent `Velocity`/`Heading`/`Possession`/`Tuning` no longer readable (the 7 sites migrated to `assumedOppSpeed`/`assumedOppTurn`); `Seed()` replaced by `NoiseSalt(id)`; `PlayerStats`→`PlayerTuning` rename; CancelCharge invariant comments corrected at all 3 sites (`intent.go`, `match.go`, `human.go`); ability exclusivity (`ai.go enforceAbilityExclusivity`: Trap>Push>Shoot, one per tick, cancel-on-takeover) kept & tested (B.6); boundary + charge-rate + determinism tests green.
- [ ] AI quality: dead tuning fields removed (`shootGap`/`turnTrapSettled`/`trapApproachFactor`; keep `cornerInset`), magic numbers centralized in `aiTuning`, `onball.go` (535 lines) scoring split into pure testable functions, nil/empty paths hardened — behaviour unchanged (harness-verified across >=6 seeds incl. 6-a-side).
- [ ] Production hardening: `config.Tuning` authoritative across the 6 `Collide` calls + `NewBall`/confine helpers (no golden diff); `View.BallFriction()` reads live ball friction; render `Viewport` (no globals); `Client.Send` race fixed; versioned enveloped netcode with handshake/deadlines/intent-expiry/keepalive; `Settings.Config()` surfaces errors; single validator; `config.DefaultEvictGrace`; `control.ValidSkill`/`SkillNames` (drop `config.validDifficulty`); difficulty/camera name drift reconciled; `internal/cliutil`; `Makefile` + GitHub Actions CI; `go.mod` toolchain pinned; README updated.
- [ ] Tests: golden replay (FIRST), `geom`/`physics`/`config`/`netcode`/`render`/`logging` units, stat-accuracy scenarios, View-excludes-stats boundary, charge-rate, determinism replays, AI-vs-AI sweeps incl. 6-a-side.
- [ ] All four HARD CONSTRAINTS hold: physics feel unchanged (golden green), AI<=human enforced & tested, determinism preserved (no `time.`/unseeded `rand.` in sim/AI — verify by grep), all tests + vet + `-race` + headless deps check green.
- [ ] Scoped commits on a feature branch (not `main`), each with the `Co-Authored-By` trailer; final summary of what changed per workstream and the before/after AI-harness stat numbers.

Begin with Workstream F.1 (golden replay test) and the D-P0 items, then proceed through the phase order in the playbook. Read the real source before editing — re-derive every line number above from the live tree. When any golden trace diffs, you have changed the feel — stop and revert.
